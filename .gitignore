node_modules
.next
out
build
dist

.env
.env.local
.env.*.local
.certi-design.png
handoff-remaining-work.md
claude.md
*.log
.DS_Store
*.csv
next-env.d.ts
.vercel
.env*
.env*.local

# Local design reference — never commit (owner request)
certi-design.png

# Third-party design skill toolkit, vendored locally. Not app code, and its
# own CLI sources break `tsc --noEmit` for the whole project.
ui-ux-pro-max-skill/

# TypeScript incremental build cache — regenerated on every build
tsconfig.tsbuildinfo
